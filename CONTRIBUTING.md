# Contributing to SAYANOX FORGE

1. Fork or create a feature branch.
2. Keep changes focused and documented.
3. Prefer deterministic, testable logic.
4. Never commit credentials, tokens, private keys, or personal data.
5. Run `npm run lint`, `npm test`, and `npm run build` before opening a PR.

Security-sensitive changes should include tests and a short threat-model note.
